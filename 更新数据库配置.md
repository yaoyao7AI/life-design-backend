# ✅ 更新数据库配置

## 📋 数据库连接信息

**已确认的信息：**
- **数据库主机：** `rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com`
- **端口：** `3306`
- **数据库名称：** `life_design`
- **用户名：** `life_design_user`
- **密码：** `YOUR_DB_PASSWORD`

---

## 🔧 更新后端配置步骤

### 步骤 1：SSH 连接到服务器

```bash
ssh -i ~/Desktop/ssh-keys/life-design-key.pem root@123.56.17.118
```

---

### 步骤 2：编辑环境变量文件

```bash
cd /root/life-design-backend
nano .env
```

---

### 步骤 3：更新数据库配置

**找到并更新以下配置：**

```env
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
```

**保存文件：**
- 按 `Ctrl + O` 保存
- 按 `Enter` 确认
- 按 `Ctrl + X` 退出

---

### 步骤 4：重启后端服务

```bash
pm2 restart life-design-backend
```

---

### 步骤 5：查看日志确认

```bash
pm2 logs life-design-backend --lines 20
```

**应该看到：**
- ✅ 服务启动成功
- ✅ 数据库连接成功
- ❌ 没有连接错误

---

## ⚠️ 重要：添加白名单

**在更新配置之前，必须先添加白名单！**

### 在 RDS 控制台添加白名单：

1. **进入实例详情页**
   - 实例ID：`rm-2ze0yppdih8t4t4e4`
   - 点击左侧菜单："白名单与安全组"

2. **添加后端服务器 IP**
   - 点击"添加白名单分组"或"修改"
   - 添加 IP：`123.56.17.118`
   - 或添加 `0.0.0.0/0`（允许所有 IP，仅用于测试）

3. **保存配置**

---

## 🧪 测试数据库连接

**在服务器上测试：**

```bash
mysql -h rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com -u life_design_user -p life_design
```

**输入密码：** `YOUR_DB_PASSWORD`

**如果连接成功，说明配置正确。**

---

## ✅ 验证步骤

### 1. 测试登录功能

**在前端：**
1. 重新获取验证码
2. 立即输入验证码并登录
3. 应该可以成功登录

---

### 2. 检查后端日志

```bash
pm2 logs life-design-backend --lines 50
```

**应该看到：**
- ✅ 登录请求成功
- ✅ 数据库查询成功
- ❌ 没有连接错误

---

## 📋 操作清单

- [ ] 添加白名单（RDS 控制台）
- [ ] SSH 连接到服务器
- [ ] 编辑 `.env` 文件
- [ ] 更新数据库配置
- [ ] 保存文件
- [ ] 重启后端服务
- [ ] 查看日志确认
- [ ] 测试登录功能

---

## 🎯 快速操作命令

**一键更新配置（在服务器上执行）：**

```bash
cd /root/life-design-backend
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
EOF
pm2 restart life-design-backend
pm2 logs life-design-backend --lines 20
```

---

**现在开始操作！先添加白名单，然后更新后端配置！** 🚀


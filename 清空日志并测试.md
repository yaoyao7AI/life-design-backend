# 🧪 清空日志并测试

## ✅ 当前状态

- ✅ **server.js 已修复：** dotenv 加载顺序已调整
- ⚠️ **错误日志可能是历史记录**

---

## 🧪 测试步骤

### 步骤 1：清空日志

**在服务器上执行：**

```bash
cd /root/apps/life-design-backend

# 清空 PM2 日志
pm2 flush life-design-backend

echo "✅ 日志已清空"
```

---

### 步骤 2：测试登录功能

**在前端：**
1. 打开浏览器，访问：`http://localhost:5173/me`
2. 输入手机号：`18210827464`
3. 点击"获取验证码"
4. 输入验证码并登录

---

### 步骤 3：查看新的日志

**在服务器上执行：**

```bash
cd /root/apps/life-design-backend

# 等待几秒，让请求完成
sleep 3

# 查看最新的错误日志
echo "=== 最新的错误日志 ==="
tail -10 /root/.pm2/logs/life-design-backend-error.log

# 查看最新的输出日志
echo ""
echo "=== 最新的输出日志 ==="
tail -10 /root/.pm2/logs/life-design-backend-out.log
```

---

## 🎯 一键测试命令

**复制以下全部命令，一次性执行：**

```bash
cd /root/apps/life-design-backend && \
echo "=== 清空日志 ===" && \
pm2 flush life-design-backend && \
echo "✅ 日志已清空" && \
echo "" && \
echo "📋 现在请在前端测试登录功能：" && \
echo "1. 访问 http://localhost:5173/me" && \
echo "2. 输入手机号并获取验证码" && \
echo "3. 输入验证码并登录" && \
echo "" && \
echo "等待 10 秒后查看日志..." && \
sleep 10 && \
echo "" && \
echo "=== 最新的错误日志 ===" && \
tail -10 /root/.pm2/logs/life-design-backend-error.log && \
echo "" && \
echo "=== 最新的输出日志 ===" && \
tail -10 /root/.pm2/logs/life-design-backend-out.log
```

---

## ✅ 预期结果

**如果修复成功，应该看到：**
- ✅ 没有 `ENOTFOUND` 错误
- ✅ 登录请求成功
- ✅ 数据库连接成功

**如果还有错误，应该看到：**
- ❌ 新的 `ENOTFOUND` 错误（说明问题仍然存在）
- ❌ 或其他错误信息

---

## 📋 操作清单

- [ ] 清空日志
- [ ] 在前端测试登录功能
- [ ] 查看新的日志
- [ ] 确认是否还有错误

---

**现在清空日志，然后在前端测试登录功能！** 🚀


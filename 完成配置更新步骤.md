# ✅ 完成配置更新步骤

## ✅ 当前状态

- ✅ **白名单已配置：** `123.56.17.118` 已在白名单中
- ✅ **数据库连接信息已确认：**
  - 主机：`rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com`
  - 端口：`3306`
  - 数据库：`life_design`
  - 用户名：`life_design_user`
  - 密码：`YOUR_DB_PASSWORD`

---

## 🔧 下一步：更新后端配置

### 步骤 1：SSH 连接到服务器

```bash
ssh -i ~/Desktop/ssh-keys/life-design-key.pem root@123.56.17.118
```

---

### 步骤 2：更新数据库配置

**在服务器上执行：**

```bash
cd /root/life-design-backend

# 备份现有配置
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)

# 更新数据库配置
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
EOF

# 重启后端服务
pm2 restart life-design-backend

# 查看日志
pm2 logs life-design-backend --lines 20
```

---

### 步骤 3：验证配置

**测试数据库连接：**

```bash
mysql -h rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com -u life_design_user -p life_design
```

**输入密码：** `YOUR_DB_PASSWORD`

**如果连接成功，说明配置正确。**

---

### 步骤 4：测试登录功能

**在前端：**
1. 重新获取验证码
2. 立即输入验证码并登录
3. 应该可以成功登录

---

## 📋 操作清单

- [x] 白名单已配置
- [ ] SSH 连接到服务器
- [ ] 更新 `.env` 文件
- [ ] 重启后端服务
- [ ] 查看日志确认
- [ ] 测试登录功能

---

## 🎯 快速操作命令

**一键更新配置（在服务器上执行）：**

```bash
cd /root/life-design-backend && \
cp .env .env.backup.$(date +%Y%m%d_%H%M%S) && \
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
EOF
pm2 restart life-design-backend && \
sleep 3 && \
pm2 logs life-design-backend --lines 20
```

---

**现在更新后端配置，然后测试登录功能！** 🚀


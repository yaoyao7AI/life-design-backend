# 🚀 在新实例下创建 life_design 数据库

## 🎯 目标
在实例 `rm-2ze0yppdih8t4t4e4` 下创建 `life_design` 数据库和 `users` 表（包含 `password` 字段）

---

## 📋 执行步骤（2种方法）

### 方法 1：通过 DMS 图形界面创建（推荐）

#### 步骤 1：切换到新实例

1. **在左侧数据库实例树中**
   - 找到实例：`rm-2ze0yppdih8t4t4e4`
   - **点击实例名称**，展开实例

---

#### 步骤 2：创建数据库

1. **右键点击实例 `rm-2ze0yppdih8t4t4e4`**
   - 选择"创建数据库"或"新建数据库"
   - 或点击实例旁边的"+"按钮

2. **填写数据库信息：**
   - **数据库名称：** `life_design`
   - **字符集：** `utf8mb4`（推荐）或 `utf8`
   - **授权账号：** 选择你的数据库账号
   - **备注：** （可选）`用于生活设计应用`

3. **点击"创建"按钮**

---

#### 步骤 3：切换到新数据库

1. **在左侧数据库树中**
   - 找到实例：`rm-2ze0yppdih8t4t4e4`
   - 找到数据库：`life_design`
   - **点击 `life_design` 数据库**

---

#### 步骤 4：创建 users 表

**在 SQL 控制台执行：**

```sql
USE life_design;

CREATE TABLE users (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
  phone VARCHAR(20) NOT NULL UNIQUE COMMENT '手机号',
  password VARCHAR(255) NULL COMMENT '密码',
  nickname VARCHAR(50) COMMENT '昵称',
  avatar VARCHAR(255) COMMENT '头像URL',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

DESCRIBE users;
```

**点击"执行(F8)"**

---

### 方法 2：通过 SQL 直接创建（更快）

#### 步骤 1：切换到新实例

1. **在左侧数据库实例树中**
   - 找到实例：`rm-2ze0yppdih8t4t4e4`
   - **点击实例名称**

---

#### 步骤 2：打开 SQL 控制台

**点击顶部的 "SQLConsole" 标签页**

---

#### 步骤 3：执行完整 SQL（一键创建）

**在 SQL 控制台执行：**

```sql
-- 1. 创建数据库
CREATE DATABASE IF NOT EXISTS life_design CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 2. 切换到新创建的数据库
USE life_design;

-- 3. 创建 users 表（包含 password 字段）
CREATE TABLE IF NOT EXISTS users (
  id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
  phone VARCHAR(20) NOT NULL UNIQUE COMMENT '手机号',
  password VARCHAR(255) NULL COMMENT '密码',
  nickname VARCHAR(50) COMMENT '昵称',
  avatar VARCHAR(255) COMMENT '头像URL',
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='用户表';

-- 4. 验证数据库创建成功
SHOW DATABASES LIKE 'life_design';

-- 5. 验证表创建成功
SHOW TABLES;

-- 6. 查看表结构（确认 password 字段存在）
DESCRIBE users;
```

**点击"执行(F8)"**

---

## ✅ 执行后的结果

**执行成功后，应该看到：**

1. **数据库创建成功**
   - `SHOW DATABASES LIKE 'life_design';` 显示 `life_design`

2. **表创建成功**
   - `SHOW TABLES;` 显示 `users`

3. **表结构正确**
   - `DESCRIBE users;` 显示所有字段，包括：
     - `id`（主键）
     - `phone`（手机号，唯一）
     - `password`（密码，可为空）✅
     - `nickname`（昵称）
     - `avatar`（头像）
     - `created_at`（创建时间）

---

## 🔍 验证清单

执行后检查：

- [ ] `SHOW DATABASES;` 能看到 `life_design`
- [ ] `SHOW TABLES;` 能看到 `users`
- [ ] `DESCRIBE users;` 能看到 `password` 字段
- [ ] `password` 字段类型是 `varchar(255)`
- [ ] `password` 字段允许 NULL

---

## 🚨 如果执行出错

### 错误 1：数据库已存在

**错误信息：** `ERROR 1007: Can't create database 'life_design'; database exists`

**解决：**
- 如果数据库已存在，直接执行：
  ```sql
  USE life_design;
  CREATE TABLE IF NOT EXISTS users (...);
  ```

---

### 错误 2：表已存在

**错误信息：** `ERROR 1050: Table 'users' already exists`

**解决：**
- 如果表已存在，检查是否有 `password` 字段：
  ```sql
  DESCRIBE users;
  ```
- 如果没有 `password` 字段，执行：
  ```sql
  ALTER TABLE users ADD COLUMN password VARCHAR(255) NULL AFTER phone;
  ```

---

### 错误 3：权限不足

**错误信息：** `ERROR 1044: Access denied`

**解决：**
- 确认使用的是高权限账号
- 联系数据库管理员授权

---

## 📋 快速操作（推荐）

**最简单的方法：**

1. **在左侧点击实例：** `rm-2ze0yppdih8t4t4e4`
2. **打开 SQL 控制台**
3. **执行上面的完整 SQL**
4. **点击"执行(F8)"**

---

## 🎯 完成标志

**执行成功后：**
- ✅ 数据库 `life_design` 在实例 `rm-2ze0yppdih8t4t4e4` 下创建成功
- ✅ 表 `users` 已创建
- ✅ `password` 字段已存在
- ✅ 表结构完整，可以正常使用

---

**现在就可以执行了！切换到新实例，执行上面的 SQL！** 🚀


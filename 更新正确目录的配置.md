# ✅ 更新正确目录的配置

## 🎯 找到问题

- ✅ **PM2 实际运行的项目路径：** `/root/apps/life-design-backend`
- ❌ **该目录下的 `.env` 文件配置错误：**
  - `DB_HOST=rm-2zec076upfs3zd44l1o.mysql.rds.aliyuncs.com` ❌
- ✅ **正确的配置应该是：**
  - `DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com` ✅

---

## 🔧 更新配置步骤

### 在服务器上执行：

```bash
cd /root/apps/life-design-backend

# 备份现有配置
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)

# 更新配置（保留其他配置，只更新数据库相关）
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
JWT_SECRET=your_secret_key_change_this_in_production
ALIYUN_ACCESS_KEY_ID=YOUR_ALIYUN_ACCESS_KEY_ID
ALIYUN_ACCESS_KEY_SECRET=YOUR_ALIYUN_ACCESS_KEY_SECRET
ALIYUN_SMS_SIGN=海口龙华陀恬雅百货商行
ALIYUN_SMS_TEMPLATE=SMS_499120253
PORT=3000
NODE_ENV=development
FRONTEND_BASE_URL=http://localhost:5174
EOF

# 验证配置
cat .env

# 重启服务
pm2 restart life-design-backend

# 等待并查看日志
sleep 5
pm2 logs life-design-backend --lines 30 --nostream
```

---

## 🎯 一键更新命令（推荐）

**复制以下全部命令，一次性执行：**

```bash
cd /root/apps/life-design-backend && \
cp .env .env.backup.$(date +%Y%m%d_%H%M%S) && \
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
JWT_SECRET=your_secret_key_change_this_in_production
ALIYUN_ACCESS_KEY_ID=YOUR_ALIYUN_ACCESS_KEY_ID
ALIYUN_ACCESS_KEY_SECRET=YOUR_ALIYUN_ACCESS_KEY_SECRET
ALIYUN_SMS_SIGN=海口龙华陀恬雅百货商行
ALIYUN_SMS_TEMPLATE=SMS_499120253
PORT=3000
NODE_ENV=development
FRONTEND_BASE_URL=http://localhost:5174
EOF
echo "✅ 配置已更新" && \
echo "" && \
echo "=== 验证配置 ===" && \
cat .env && \
echo "" && \
echo "=== 重启服务 ===" && \
pm2 restart life-design-backend && \
sleep 5 && \
echo "" && \
echo "=== 查看日志 ===" && \
pm2 logs life-design-backend --lines 30 --nostream
```

---

## ✅ 执行后检查

**配置更新后，日志应该显示：**
- ✅ 没有 `ENOTFOUND` 错误
- ✅ 没有数据库连接错误
- ✅ 服务正常运行

---

## 📋 操作清单

- [x] 找到正确的项目目录：`/root/apps/life-design-backend`
- [ ] 备份现有配置
- [ ] 更新 `.env` 文件
- [ ] 验证配置
- [ ] 重启后端服务
- [ ] 查看日志确认

---

**现在执行上面的更新命令！** 🚀


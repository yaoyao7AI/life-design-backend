# 🔍 检查服务器网络并更新数据库配置

## 📋 RDS 连接信息

**内网地址：** `rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com`  
**外网地址：** `rm-2ze0yppdih8t4t4e4vo.mysql.rds.aliyuncs.com`（注意多了 `vo`）  
**VPC：** `vpc-2zevygju648wazzy41ylp`  
**网段：** `172.16.0.0/12`  
**白名单：** ✅ 已包含 `123.56.17.118`

---

## 🔍 第一步：检查服务器网络配置

**在服务器上执行以下命令，检查服务器是否在同一个 VPC：**

```bash
# 查看服务器内网 IP
echo "=== 服务器内网 IP ==="
hostname -I | awk '{print $1}'

# 查看服务器公网 IP
echo ""
echo "=== 服务器公网 IP ==="
curl -s ifconfig.me

# 检查服务器是否在 VPC 网段内（172.16.0.0/12）
echo ""
echo "=== 检查 IP 地址范围 ==="
INTERNAL_IP=$(hostname -I | awk '{print $1}')
echo "内网 IP: $INTERNAL_IP"
if [[ $INTERNAL_IP =~ ^172\.(1[6-9]|2[0-9]|3[0-1])\..* ]]; then
  echo "✅ 服务器在 VPC 网段内（172.16.0.0/12）"
  echo "✅ 应该使用内网地址"
else
  echo "⚠️  服务器可能不在 VPC 网段内"
  echo "⚠️  可能需要使用外网地址"
fi
```

---

## ✅ 第二步：根据网络类型更新配置

### 情况 1：服务器在同一个 VPC（推荐使用内网地址）

**如果服务器内网 IP 在 `172.16.0.0/12` 范围内：**

```bash
cd /root/apps/life-design-backend

# 备份配置
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)

# 更新为内网地址
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
JWT_SECRET=your_secret_key_change_this_in_production
ALIYUN_ACCESS_KEY_ID=YOUR_ALIYUN_ACCESS_KEY_ID
ALIYUN_ACCESS_KEY_SECRET=YOUR_ALIYUN_ACCESS_KEY_SECRET
ALIYUN_SMS_SIGN=海口龙华陀恬雅百货商行
ALIYUN_SMS_TEMPLATE=SMS_499120253
PORT=3000
NODE_ENV=development
FRONTEND_BASE_URL=http://localhost:5174
EOF

echo "✅ 已更新为内网地址"
cat .env | grep DB_HOST
```

---

### 情况 2：服务器不在同一个 VPC（使用外网地址）

**如果服务器内网 IP 不在 `172.16.0.0/12` 范围内：**

```bash
cd /root/apps/life-design-backend

# 备份配置
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)

# 更新为外网地址（注意多了 vo）
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4vo.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
JWT_SECRET=your_secret_key_change_this_in_production
ALIYUN_ACCESS_KEY_ID=YOUR_ALIYUN_ACCESS_KEY_ID
ALIYUN_ACCESS_KEY_SECRET=YOUR_ALIYUN_ACCESS_KEY_SECRET
ALIYUN_SMS_SIGN=海口龙华陀恬雅百货商行
ALIYUN_SMS_TEMPLATE=SMS_499120253
PORT=3000
NODE_ENV=development
FRONTEND_BASE_URL=http://localhost:5174
EOF

echo "✅ 已更新为外网地址"
cat .env | grep DB_HOST
```

---

## 🧪 第三步：测试数据库连接

**更新配置后，测试连接：**

```bash
cd /root/apps/life-design-backend

node -e "
const mysql = require('mysql2/promise');
require('dotenv').config();

(async () => {
  try {
    console.log('=== 测试数据库连接 ===');
    console.log('DB_HOST:', process.env.DB_HOST);
    console.log('');
    
    const pool = mysql.createPool({
      host: process.env.DB_HOST,
      port: Number(process.env.DB_PORT),
      user: process.env.DB_USER,
      password: process.env.DB_PASS,
      database: process.env.DB_NAME,
      connectionLimit: 1,
      connectTimeout: 10000
    });
    
    console.log('正在连接数据库...');
    const connection = await pool.getConnection();
    console.log('✅ 数据库连接成功！');
    
    const [rows] = await connection.query('SELECT DATABASE() as db, NOW() as time');
    console.log('✅ 查询成功:', rows[0]);
    
    connection.release();
    await pool.end();
    process.exit(0);
  } catch (error) {
    console.error('❌ 数据库连接失败:');
    console.error('错误代码:', error.code);
    console.error('错误信息:', error.message);
    if (error.code === 'ETIMEDOUT') {
      console.error('');
      console.error('💡 如果使用内网地址失败，尝试使用外网地址');
      console.error('💡 如果使用外网地址失败，检查白名单配置');
    }
    process.exit(1);
  }
})();
"
```

---

## 🔄 第四步：重启后端服务

**如果连接成功，重启服务：**

```bash
cd /root/apps/life-design-backend

# 完全停止并删除 PM2 进程
pm2 stop life-design-backend
pm2 delete life-design-backend

# 重新启动（确保加载新的环境变量）
pm2 start npm --name life-design-backend -- start

# 等待并查看日志
sleep 5
pm2 logs life-design-backend --lines 30 --nostream
```

---

## 🎯 一键执行命令（推荐）

**复制以下全部命令，一次性执行：**

```bash
cd /root/apps/life-design-backend && \
echo "=== 1. 检查服务器网络 ===" && \
INTERNAL_IP=$(hostname -I | awk '{print $1}') && \
echo "内网 IP: $INTERNAL_IP" && \
if [[ $INTERNAL_IP =~ ^172\.(1[6-9]|2[0-9]|3[0-1])\..* ]]; then
  echo "✅ 服务器在 VPC 网段内，使用内网地址"
  USE_INTERNAL=true
else
  echo "⚠️  服务器可能不在 VPC 网段内，使用外网地址"
  USE_INTERNAL=false
fi && \
echo "" && \
echo "=== 2. 更新数据库配置 ===" && \
cp .env .env.backup.$(date +%Y%m%d_%H%M%S) && \
if [ "$USE_INTERNAL" = true ]; then
  DB_HOST="rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com"
  echo "使用内网地址: $DB_HOST"
else
  DB_HOST="rm-2ze0yppdih8t4t4e4vo.mysql.rds.aliyuncs.com"
  echo "使用外网地址: $DB_HOST"
fi && \
cat > .env << EOF
DB_HOST=$DB_HOST
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
JWT_SECRET=your_secret_key_change_this_in_production
ALIYUN_ACCESS_KEY_ID=YOUR_ALIYUN_ACCESS_KEY_ID
ALIYUN_ACCESS_KEY_SECRET=YOUR_ALIYUN_ACCESS_KEY_SECRET
ALIYUN_SMS_SIGN=海口龙华陀恬雅百货商行
ALIYUN_SMS_TEMPLATE=SMS_499120253
PORT=3000
NODE_ENV=development
FRONTEND_BASE_URL=http://localhost:5174
EOF
echo "✅ 配置已更新" && \
echo "" && \
echo "=== 3. 测试数据库连接 ===" && \
node -e "
const mysql = require('mysql2/promise');
require('dotenv').config();
(async () => {
  try {
    const pool = mysql.createPool({
      host: process.env.DB_HOST,
      port: Number(process.env.DB_PORT),
      user: process.env.DB_USER,
      password: process.env.DB_PASS,
      database: process.env.DB_NAME,
      connectionLimit: 1,
      connectTimeout: 10000
    });
    const connection = await pool.getConnection();
    console.log('✅ 数据库连接成功！');
    const [rows] = await connection.query('SELECT DATABASE() as db');
    console.log('✅ 当前数据库:', rows[0].db);
    connection.release();
    await pool.end();
    process.exit(0);
  } catch (error) {
    console.error('❌ 连接失败:', error.code, error.message);
    process.exit(1);
  }
})();
" && \
echo "" && \
echo "=== 4. 重启后端服务 ===" && \
pm2 stop life-design-backend 2>/dev/null || true && \
pm2 delete life-design-backend 2>/dev/null || true && \
pm2 start npm --name life-design-backend -- start && \
sleep 5 && \
pm2 logs life-design-backend --lines 30 --nostream
```

---

## 📋 操作清单

- [ ] 执行上面的检查命令，确认服务器网络类型
- [ ] 根据网络类型更新数据库配置（内网或外网地址）
- [ ] 测试数据库连接
- [ ] 如果连接成功，重启后端服务
- [ ] 测试登录功能

---

**现在执行上面的命令！** 🚀


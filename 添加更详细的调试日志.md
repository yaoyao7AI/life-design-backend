# 🔍 添加更详细的调试日志

## ❌ 问题

**第 75 行是 `getConnection()`，但错误显示 `PromisePool.query`。**

**可能的原因：**
- `getConnection()` 内部调用 `query()` 时失败
- 需要添加更详细的调试日志

---

## 🚀 在服务器上更新文件

**我已经在本地添加了更详细的调试日志，现在需要在服务器上更新：**

```bash
cd /root/apps/life-design-backend

# 备份
cp src/routes/auth.js src/routes/auth.js.backup.$(date +%Y%m%d_%H%M%S)

# 打开文件编辑
nano src/routes/auth.js
```

**找到第 73 行的 `try {`，在 `const connection = await pool.getConnection();` 之前添加：**

```javascript
    console.log('[登录] 开始获取连接，pool状态:', pool.pool ? '已创建' : '未创建');
    console.log('[登录] 当前时间:', new Date().toISOString());
```

**保存文件（Ctrl+O, Enter, Ctrl+X），然后重启服务：**

```bash
pm2 restart life-design-backend --update-env
sleep 5
pm2 logs life-design-backend --lines 30 --nostream | grep -E "\[登录\]|ETIMEDOUT"
```

---

**或者，让我提供一个完整的替换方案。**


# 🔧 修复连接池配置并重启

## ✅ 测试结果确认

**从测试确认：**
- ✅ 单个 MySQL 连接成功！
- ✅ 连接池连接成功！
- ⚠️ 警告：`acquireTimeout` 和 `timeout` 不是 Connection 的有效配置选项

**数据库连接正常！但应用仍然报错，可能是因为连接池配置中有无效选项。**

---

## 🔧 已修复的配置

**我已经更新了 `src/db.js`，移除了无效的配置选项：**
- ❌ 移除 `acquireTimeout`（不是 Connection 的有效选项）
- ❌ 移除 `timeout`（不是 Connection 的有效选项）
- ✅ 保留 `connectTimeout: 30000`（连接超时）
- ✅ 保留 `enableKeepAlive: true`（启用 keep-alive）

---

## 🚀 部署步骤

### 在服务器上执行：

```bash
cd /root/apps/life-design-backend

# 备份原文件
cp src/db.js src/db.js.backup.$(date +%Y%m%d_%H%M%S)

# 更新文件（移除无效配置选项）
cat > src/db.js << 'EOF'
import mysql from "mysql2/promise";
import dotenv from "dotenv";

// 确保环境变量已加载（server.js 已经加载，这里再次确保）
dotenv.config();

// 添加调试日志，确认环境变量是否正确加载
console.log('[DB] 创建连接池，DB_HOST:', process.env.DB_HOST);
console.log('[DB] DB_PORT:', process.env.DB_PORT);
console.log('[DB] DB_USER:', process.env.DB_USER);
console.log('[DB] DB_NAME:', process.env.DB_NAME);

export const pool = mysql.createPool({
  host: process.env.DB_HOST,
  port: Number(process.env.DB_PORT),
  user: process.env.DB_USER,
  password: process.env.DB_PASS,
  database: process.env.DB_NAME,
  connectionLimit: 10,
  connectTimeout: 30000, // 连接超时时间（30 秒）
  enableKeepAlive: true, // 启用 keep-alive
  keepAliveInitialDelay: 0, // keep-alive 初始延迟
  // 注意：acquireTimeout 和 timeout 不是 Connection 的有效选项，已移除
});
EOF

# 验证配置
echo "=== 验证更新后的配置 ==="
cat src/db.js | grep -A 8 "createPool"

# 重启服务
echo ""
echo "=== 重启服务 ==="
pm2 stop life-design-backend
pm2 delete life-design-backend
pm2 save --force
sleep 3
pm2 start src/server.js --name life-design-backend --update-env
sleep 5
pm2 logs life-design-backend --lines 30 --nostream
```

---

## 🎯 一键部署命令（推荐）

**复制以下全部命令，一次性执行：**

```bash
cd /root/apps/life-design-backend && \
echo "=== 1. 备份并更新配置 ===" && \
cp src/db.js src/db.js.backup.$(date +%Y%m%d_%H%M%S) && \
cat > src/db.js << 'EOF'
import mysql from "mysql2/promise";
import dotenv from "dotenv";

dotenv.config();

console.log('[DB] 创建连接池，DB_HOST:', process.env.DB_HOST);
console.log('[DB] DB_PORT:', process.env.DB_PORT);
console.log('[DB] DB_USER:', process.env.DB_USER);
console.log('[DB] DB_NAME:', process.env.DB_NAME);

export const pool = mysql.createPool({
  host: process.env.DB_HOST,
  port: Number(process.env.DB_PORT),
  user: process.env.DB_USER,
  password: process.env.DB_PASS,
  database: process.env.DB_NAME,
  connectionLimit: 10,
  connectTimeout: 30000,
  enableKeepAlive: true,
  keepAliveInitialDelay: 0,
});
EOF
echo "✅ 配置已更新" && \
echo "" && \
echo "=== 2. 重启服务 ===" && \
pm2 stop life-design-backend 2>/dev/null || true && \
pm2 delete life-design-backend 2>/dev/null || true && \
pm2 save --force && \
sleep 3 && \
pm2 start src/server.js --name life-design-backend --update-env && \
sleep 5 && \
echo "" && \
echo "=== 3. 查看日志 ===" && \
pm2 logs life-design-backend --lines 30 --nostream
```

---

## ✅ 预期结果

**重启后，应该看到：**
- ✅ 没有配置警告
- ✅ 服务启动成功
- ✅ 日志中没有 `ETIMEDOUT` 错误
- ✅ 可以正常登录

---

## 📋 操作清单

- [x] 测试确认数据库连接正常
- [x] 修复连接池配置（移除无效选项）
- [ ] 部署更新后的配置
- [ ] 重启服务并测试
- [ ] 测试登录功能

---

**现在执行上面的部署命令！** 🚀


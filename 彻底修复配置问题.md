# 🔧 彻底修复配置问题

## ❌ 问题确认

**日志显示的错误：**
```
Error: getaddrinfo ENOTFOUND rm-2zec076upfs3zd44l1o.mysql.rds.aliyuncs.com
```

**正确的地址应该是：**
```
rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
```

**说明配置没有正确更新！**

---

## 🔍 排查步骤

### 步骤 1：检查当前配置

**在服务器上执行：**

```bash
cd /root/life-design-backend
echo "=== 当前 .env 文件内容 ==="
cat .env
echo ""
echo "=== DB_HOST 的值 ==="
grep DB_HOST .env
```

---

### 步骤 2：检查项目目录

**确认项目目录是否正确：**

```bash
pwd
ls -la
```

**注意：** 日志显示路径是 `/root/apps/life-design-backend`，但我们在操作 `/root/life-design-backend`

---

### 步骤 3：检查实际运行的项目目录

**从日志看，实际运行的项目可能在：**

```bash
cd /root/apps/life-design-backend
cat .env
```

---

## ✅ 解决方案

### 方案 1：更新正确的项目目录

**如果项目在 `/root/apps/life-design-backend`：**

```bash
cd /root/apps/life-design-backend

# 备份
cp .env .env.backup.$(date +%Y%m%d_%H%M%S)

# 更新配置
cat > .env << 'EOF'
DB_HOST=rm-2ze0yppdih8t4t4e4.mysql.rds.aliyuncs.com
DB_PORT=3306
DB_USER=life_design_user
DB_PASS=YOUR_DB_PASSWORD
DB_NAME=life_design
EOF

# 验证
cat .env

# 重启
pm2 restart life-design-backend
```

---

### 方案 2：检查 PM2 启动路径

**查看 PM2 启动配置：**

```bash
pm2 show life-design-backend
```

**查看启动脚本路径。**

---

## 🎯 完整排查命令

**复制以下全部命令，一次性执行：**

```bash
echo "=== 检查项目目录 ===" && \
echo "当前目录：" && \
pwd && \
echo "" && \
echo "=== 检查 /root/life-design-backend ===" && \
if [ -f /root/life-design-backend/.env ]; then \
  echo "找到 .env 文件" && \
  cat /root/life-design-backend/.env; \
else \
  echo "未找到 .env 文件"; \
fi && \
echo "" && \
echo "=== 检查 /root/apps/life-design-backend ===" && \
if [ -f /root/apps/life-design-backend/.env ]; then \
  echo "找到 .env 文件" && \
  cat /root/apps/life-design-backend/.env; \
else \
  echo "未找到 .env 文件"; \
fi && \
echo "" && \
echo "=== PM2 进程信息 ===" && \
pm2 show life-design-backend | grep -E "script path|exec cwd"
```

---

## 📋 操作清单

- [ ] 检查项目目录路径
- [ ] 找到正确的 .env 文件位置
- [ ] 更新正确的 .env 文件
- [ ] 重启后端服务
- [ ] 查看日志确认

---

**现在执行上面的排查命令，找到正确的项目目录！** 🔍

